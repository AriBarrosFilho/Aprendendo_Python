001) Como contar a quantidade de ocorrências de um determinado conjunto de caracteres dentro de um texto usando a biblioteca regex?	padrao = re.compile("conjunto de caracteres qualquer") <br> resultado = re.findall(padrao, texto) <br> len(resultado)
002) Como encontrar a posição inicial e final da primeira ocorrência de um determinado conjunto de caracteres dentro de um texto usando a biblioteca regex?	padrao = re.compile("conjunto de caracteres qualquer") <br> resultado = re.search(padrao, texto) <br> resultado.span()
003) Como obter o que realmente foi encontrado da primeira ocorrência de um determinado conjunto de caracteres dentro de um texto usando a biblioteca regex?	padrao = re.compile("conjunto de caracteres qualquer") <br> resultado = re.search(padrao, texto) <br> resultado.group()
004) Como obter uma lista de tuplas que representa a posição inicial e final de todas as ocorrências de um determinado conjunto de caracteres dentro de um texto usando a biblioteca regex?	padrao = re.compile("bebida") <br> resultado = re.finditer(padrao, texto) <br> lista_posicoes = [item.span() for item in resultado]