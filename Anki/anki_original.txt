Como deletar um arquivo ou pasta, mesmo que a pasta não esteja vazia utilizando o shutil? shutil.rmtree(caminho)
Como copiar um arquivou usando shutil? shutil.copy(src = "Biblioteca time.ipynb", dst = "images_markdown") <br><br> A pasta de destino já precisa existir
Como copiar os arquivos e pastas de uma pasta usando shutil? shutil.copytree(src = r"C:\Users\AriosvaldoBarros\Desktop\Ferramentas Computacionais", dst = "teste") <br><br> A pasta de destino já precisa existir