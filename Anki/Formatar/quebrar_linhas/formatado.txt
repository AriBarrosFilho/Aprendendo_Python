for i, valor in enumerate(y):<br>    plt.annotate(text = np.round(a = x[i], decimals = 2), # Define o texto que vai aparecer<br>                 xy = (x[i], valor), xycoords = 'data', # Define onde ficaria a ponta da possível seta<br>                 xytext = (x[i] + 1, valor), textcoords = 'data', # Define onde vai ficar o texto<br>                 bbox = dict(boxstyle = "Square", fc="w", edgecolor = "r", ls = "--"),<br>                 arrowprops = dict(arrowstyle = "-|>", connectionstyle = "arc3,rad=-0.1", fc = "k"))